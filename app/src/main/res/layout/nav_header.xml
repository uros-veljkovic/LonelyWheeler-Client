<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="200dp"
    android:background="@color/colorPrimary"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:layout_marginTop="32dp"
        android:id="@+id/ivLonelyWheelerIconPlaceholder"
        android:src="@mipmap/ic_lonelywheeler_orange_background"
        android:layout_width="100dp"
        android:layout_height="100dp"/>
    <TextView
        android:id="@+id/tvLonelyWheelerPlaceholder"
        android:layout_width="wrap_content"
        style="@style/TextAppearance.LonelyWheeler.Title.NavDrawer"
        android:textColor="@color/defaultBackgroundColor"
        android:text="Lonely Wheeler"
        android:layout_height="wrap_content"/>


</LinearLayout>